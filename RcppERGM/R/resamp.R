#' @export
resamp <- function(steps, simnet, industries, violation, coef)
{
    resamp_(steps, simnet, industries, violation, coef)
}
#' @export
resamp1 <- function(steps, simnet, industries, violation, coef)
{
    resamp1_(steps, simnet, industries, violation, coef)
}
